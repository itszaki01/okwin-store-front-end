export interface IStorePageRES {
    title: string;
    pageIndex: number;
    showPage: boolean;
    slug: string;
    section?: string;
    body: string;
    store: string;
    createdAt: string;
    updatedAt: string;
    _id: string;
    id: string;
}
